modules:
  - provider:
      type: clock
      clock_format: "15:04"
      clock_format_alternate: "2006-01-02"
    appearance:
      format: '  {{ .Text }}'
    variants:
      - pattern: '^\d{4}-\d{2}-\d{2}$'
        appearance:
          format: '  {{ .Text }}'
